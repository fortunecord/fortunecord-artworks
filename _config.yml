# Default layout: all .md files use the default template
defaults:
  - scope:
      path: ""
    values:
      layout: default

# Enable clean URLs: /series/xxx points to /series/xxx.md
permalink: /:path/
